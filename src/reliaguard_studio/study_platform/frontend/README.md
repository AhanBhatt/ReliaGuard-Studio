# ReliaGuard-NS Prospective Study Frontend Placeholder

The deployable study API is implemented in `study_platform/app.py`. This folder is reserved for a static participant-facing frontend once an IRB-approved deployment target is selected.

No participant recruitment is enabled by default.
