{
  "name": "@reliaguard/studio",
  "version": "0.1.0",
  "description": "TypeScript SDK for streaming human-AI decision events into ReliaGuard Studio.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
